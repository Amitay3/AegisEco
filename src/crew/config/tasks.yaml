fetch_and_store_task:
  description: >
    You are responsible for updating the AegisEco database with the latest meteorological and hydrological data.
    You must perform three distinct steps:
    1. Use the 'Sync Rain Data' tool to fetch current measurements from IMS and save them.
    2. Use the 'Sync Flow Data' tool to fetch current river flow measurements and save them.
    3. Use the 'Update Database Forecasts' tool to fetch the 6-hour rainfall forecasts.
  expected_output: >
    A short summary confirming the real-time data syncs (rain and flow) and the forecast updates.
  agent: data_engineer

analyze_risk_task:
  description: >
    Use the 'Run ML Inference on All Basins' tool to execute the XGBoost predictive models on the real-time data.
    Review the generated ML report carefully. If any basin shows a 'CRITICAL ALERT', flag it immediately and note the exact probability.
    If all basins are normal, state clearly that no alerts were triggered. Pass this critical information to the Communications Officer.
  expected_output: >
    A summary of the ML inference results, highlighting any basins that have triggered a flash flood alert, including their specific probabilities.
  agent: flood_analyst

alert_task:
  description: >
    Step 1: Call the 'Get Alert Plan' tool first. It compares the latest ML
    results against the alerts already sent and tells you exactly which
    basins need a new warning, which need an all-clear, and which need no
    action at all. Treat its output as the source of truth for WHICH basins
    to message — the OSINT Ground-Truth Report and ML Inference Report are
    only used to add VERIFICATION CONTEXT to a message you are already
    sending, never to add or remove basins from the plan.

    Step 2: If "NEW FLOOD WARNINGS TO SEND" lists one or more basins:
    Draft and send ONE emergency message via the 'Send Telegram Alert' tool
    covering ALL of those basins, following the rules below. Then call the
    'Log Sent Alert' tool once for EACH basin in that list, passing its exact
    name and probability from the plan with alert_type='flood_warning'.

    Rules for the emergency message:
    1. EXACT LOCATION: The message MUST clearly state the Basin Name and region for each basin at the top.
    2. FORMATTING: Use HTML tags for emphasis (e.g., <b>Bold</b> or <i>Italic</i>).
    3. DETAILS: Include the exact probability from the plan for each basin. If a basin is marked ESCALATION, explicitly say the risk has increased since the previous warning.
    4. VERIFICATION: State clearly if the threat is a mathematical prediction, or if it was also confirmed by the OSINT Ground-Truth Report.
    5. ROADS: For each basin in this list, you MUST call the 'Get Affected Roads for Basin' tool using its exact main basin name. Include the returned road names under a clearly labelled section (e.g., <b>⚠️ Affected Roads:</b>). If the tool returns no roads, omit this section for that basin.
    6. ACTION: Suggest immediate safety actions (e.g., "Avoid roads crossing this basin").

    Step 3: If "ALL-CLEAR NOTICES TO SEND" lists one or more basins:
    Draft and send ONE SEPARATE message via the 'Send Telegram Alert' tool
    covering ALL of those basins, clearly stating that the flood warning for
    each named basin has been lifted and conditions have returned to normal
    (include its current probability from the plan). Then call the
    'Log Sent Alert' tool once for EACH basin in that list, passing its exact
    name and current probability with alert_type='all_clear'.

    Step 4: Basins listed under "NO ACTION" must NOT be mentioned in any
    Telegram message and must NOT be passed to 'Log Sent Alert'.

    Step 5: If BOTH "NEW FLOOD WARNINGS TO SEND" and "ALL-CLEAR NOTICES TO
    SEND" are empty, do NOT call 'Send Telegram Alert' or 'Log Sent Alert' at
    all. Simply output the text "System Normal: No alert changes."
  expected_output: >
    A summary of which messages (if any) were sent via 'Send Telegram Alert'
    and which basins were recorded via 'Log Sent Alert', OR the text
    "System Normal: No alert changes." if the alert plan required no action.
  agent: communications_officer
  
verify_rss_task:
  description: >
    Use the 'Search Israeli News RSS Feeds' tool to scan Ynet, Walla, Mako, and Times of Israel
    for flood-related articles published in the last 24 hours.

    CRITICAL TIME RULE: Only report articles that are genuinely from today. If the article
    date is from a previous day, explicitly state it is outdated and discard it.

    Report each article with its source, headline, and link. If nothing is found, state clearly
    that no flood coverage was found in Israeli news feeds today.
  expected_output: >
    A structured list of fresh Israeli news articles mentioning floods, with source, title, and link.
    Or a clear statement that no relevant articles were found in the past 24 hours.
  agent: rss_analyst

verify_telegram_task:
  description: >
    Use the 'Search Telegram Emergency Channels' tool to check public Israeli emergency
    Telegram channels for flood-related messages posted in the last 24 hours.

    Focus on messages from official channels (IDF Home Front Command, Magen David Adom)
    and weather alert channels. Only report messages that indicate an ACTIVE or IMMINENT
    flood threat — not weather forecasts or historical posts.

    If the tool returns a session error, report that Telegram monitoring is unavailable
    and continue without it.
  expected_output: >
    A list of relevant Telegram messages with channel name, time, and content.
    Or a clear statement that no active flood alerts were found in monitored channels.
  agent: telegram_analyst

verify_floods_task:
  description: >
    Review the ML Inference Report produced by the Hydrological Analyst. 
    Use the 'Search Web and News for Floods' tool to look for recent real-world reports.
    
    CRITICAL TIME RULE: You must strictly verify the dates of the search results. 
    Ignore any news snippets, articles, or reports that are from previous years (e.g., 2023, 2024, 2025) or previous months. 
    If a snippet talks about a past date, explicitly state "Found reports, but they are outdated."
    
    If no fresh alerts were triggered, do a general search just in case the model missed something.
  expected_output: >
    A Ground-Truth Verification Report detailing only FRESH, CURRENT real-world news confirming the floods, or a clear statement that no human reports have been sighted today.
  agent: osint_analyst

monitor_warnings_task:
  description: >
    Use the 'Fetch IMS Warnings' tool to check for active weather warnings.
    You MUST format the output EXACTLY according to the strict template below. 
    There may be multiple warnings. Treat every warning as a separate item.
    If the tool returns a message of no active warnings (e.g., "No active warnings currently in the IMS feed") return that message directly.
    Extract the warning type directly from the alert text and format it in Title Case (e.g., Flash Floods).

    TEMPLATE:
    # {Date}

    ## {Color} - {Warning Type}
    Regions: {warning regions}
    Timeframe: {warning time and dates}

    EXAMPLE:
    # 07/04/2026

    ## Yellow - Flash Floods
    Regions: Judea Desert, Dead Sea, Northern Arava
    Timeframe: 10:00 to 22:00 LT

    ## Orange - Flash Floods
    Regions: Southern Negev
    Timeframe: 12:00 to 20:00 LT
  expected_output: >
    A strict string block using Markdown headers (# for Date, ## for Color and Type), with each warning structured clearly and separated by an empty line.
  agent: warning_monitor



